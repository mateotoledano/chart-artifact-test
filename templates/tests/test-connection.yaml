apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "chart-teo.fullname" . }}-test-connection"
  labels:
    {{- include "chart-teo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "chart-teo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
